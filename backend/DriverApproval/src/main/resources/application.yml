spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/driver_approval_db
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8081